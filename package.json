{
  "name": "laya_texture_packer",
  "version": "2.0.13",
  "description": "An node version texturePacker base on codes in Egret Wing 3's res modle plugin.",
  "bin": {
    "laya_texture_packer": "./cli.js"
  },
  "index": "./dist/Main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./dist/Main.js",
    "prepare": "tsc && npm version patch && git push && git push --tags"
  },
  "author": "terran.tian",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^9.6.55",
    "canvas": "^2.6.1",
    "i": "^0.3.6",
    "npm": "^6.14.4",
    "probe-image-size": "^3.2.0"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
